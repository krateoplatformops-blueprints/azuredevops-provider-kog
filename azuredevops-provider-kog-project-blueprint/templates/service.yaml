apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-plugin-chart.fullname" . }}
  labels:
    {{- include "project-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "project-plugin-chart.selectorLabels" . | nindent 4 }}
