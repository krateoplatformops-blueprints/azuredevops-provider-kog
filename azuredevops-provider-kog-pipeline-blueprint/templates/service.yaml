apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipeline-plugin-chart.fullname" . }}
  labels:
    {{- include "pipeline-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipeline-plugin-chart.selectorLabels" . | nindent 4 }}
