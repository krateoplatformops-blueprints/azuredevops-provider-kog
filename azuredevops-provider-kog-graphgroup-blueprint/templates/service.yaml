apiVersion: v1
kind: Service
metadata:
  name: {{ include "graphgroup-plugin-chart.fullname" . }}
  labels:
    {{- include "graphgroup-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "graphgroup-plugin-chart.selectorLabels" . | nindent 4 }}
