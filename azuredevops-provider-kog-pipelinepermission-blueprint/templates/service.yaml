apiVersion: v1
kind: Service
metadata:
  name: {{ include "pipelinepermission-plugin-chart.fullname" . }}
  labels:
    {{- include "pipelinepermission-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pipelinepermission-plugin-chart.selectorLabels" . | nindent 4 }}
