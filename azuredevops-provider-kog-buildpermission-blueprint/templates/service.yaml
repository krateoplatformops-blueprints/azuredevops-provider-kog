apiVersion: v1
kind: Service
metadata:
  name: {{ include "buildpermission-plugin-chart.fullname" . }}
  labels:
    {{- include "buildpermission-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "buildpermission-plugin-chart.selectorLabels" . | nindent 4 }}
