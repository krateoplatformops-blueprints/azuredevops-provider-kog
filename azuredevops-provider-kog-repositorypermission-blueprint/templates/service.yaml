apiVersion: v1
kind: Service
metadata:
  name: {{ include "repositorypermission-plugin-chart.fullname" . }}
  labels:
    {{- include "repositorypermission-plugin-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "repositorypermission-plugin-chart.selectorLabels" . | nindent 4 }}
